{
  "name": "yintian-lineage-official",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/prepare-public.cjs",
    "build": "vite build",
    "postbuild": "node scripts/finalize-dist.cjs",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.45.1",
    "vite": "^8.1.5",
    "wrangler": "^4.112.0"
  }
}
